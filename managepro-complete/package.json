{
  "name": "managepro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:api": "cd workers && npm run dev",
    "dev:web": "cd frontend && npm run dev",
    "install:all": "cd workers && npm install && cd ../frontend && npm install",
    "build:web": "cd frontend && npm run build",
    "deploy:api": "cd workers && npx wrangler deploy",
    "db:migrate": "cd workers && npx wrangler d1 execute managepro-db --file=./schema.sql",
    "db:migrate:local": "cd workers && npx wrangler d1 execute managepro-db --local --file=./schema.sql"
  }
}
